describe('Game', function() {
  it("creates a new game with the given specifications", function(){
    var testGame= new Game();
    expect(testGame).to.equal();
    expect(testGame).to.equal();
    expect(testGame).to.equal();
    expect(testGame).to.equal();
  });

  

});
